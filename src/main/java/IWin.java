public interface IWin {
    //checkWinner method: returns the winning player (assume single player for now)
    public String checkWinner();

    //have a tie option which forces the players to replay until a winner is found

    //or return a boolean for indiv player

   // public boolean checkWinner(Player player);
}
